{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "./build/server.js",
  "scripts": {
    "build": "tsc",
    "rebuild": "rimraf ./build && tsc",
    "start:prod": "node ./build/server.js",
    "start:local": "rimraf ./build && tsc && node ./build/server.js",
    "proto:gen": "proto-loader-gen-types --grpcLib=@grpc/grpc-js --outDir=./gRPC ./gRPC/test.proto"
  },
  "author": "Ari Insomnia",
  "license": "ISC",
  "devDependencies": {
    "tslib": "^2.5.3",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.8.16",
    "@grpc/proto-loader": "^0.7.7",
    "node-quic": "^0.1.3",
    "quic": "^0.4.2",
    "rimraf": "^5.0.1"
  }
}
